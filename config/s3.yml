development:
    bucket_name: doomhub
    access_key_id: %{Settings.secret.s3.key}
    aws_secret_access_key: %{Settings.secret.s3.access_key}

production:
    bucket_name: doomhub
    access_key_id: %{Settings.secret.s3.key}
    aws_secret_access_key: %{Settings.secret.s3.access_key}